node_modules
dist
.env
.env.local
.DS_Store
Thumbs.db
*.log

# Secrets and local tooling state — never commit
.env.*
!.env.example
.claude/
CLAUDE.md
.idea/
.vscode/
.fleet/
.junie/
*.iml
*.pem
*.p12
*.pfx
id_rsa
id_ed25519

# Vercel CLI link state (project/org ids) is per-machine.
.vercel

# Windows folder metadata
desktop.ini

# Supabase CLI scratch state (link ids, pooler URL)
supabase/.temp
